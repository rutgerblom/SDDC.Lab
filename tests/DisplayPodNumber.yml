---
- hosts: localhost
  gather_facts: false
  tasks:
    - name: Display Pod Number
      debug:
        msg: "{{ Pod.Number }}"

    - name: Display Deploy.Products.Router
      debug:
        msg: "{{ Deploy.Products.Router }}"

