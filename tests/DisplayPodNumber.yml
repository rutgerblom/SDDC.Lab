---
- hosts: localhost
  gather_facts: false
  tasks:
    - name: Display Pod Number
      debug:
        msg: "{{ Pod.Number }}"

    - name: Display Deploy.Router
      debug:
        msg: "{{ Deploy.Router }}"

