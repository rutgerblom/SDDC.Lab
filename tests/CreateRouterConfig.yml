---
- hosts: localhost
  tasks:

    - name: Create configuration file for VyOS router
      ansible.builtin.template: 
        src:  "{{ Target.TemplateFolder }}/vyos_router_commands.j2"
        dest: "/tmp/vyos_router_commands"
        mode: "666"
